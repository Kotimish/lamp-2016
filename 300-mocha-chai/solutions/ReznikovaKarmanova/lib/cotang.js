'use strict';


var cotan = function(x) {
  
  // This 'black box' function must return square of x.
  // Does it work properly ?

x = Math.cot(x);

  return x;
};


module.exports = {
  cotan: cotan
};

